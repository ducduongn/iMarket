class Property {
    constructor(name, value, ) {
        this.name = name
        this.value = value
    }
}

class Product {
    constructor(name, category, properties, tag) {
        this.name = name
        this.category = category
        this.properties = properties
        this.tag = tag
    }
}

